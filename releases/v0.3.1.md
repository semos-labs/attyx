# Attyx v0.3.1

## Bug Fixes

- **Session commands not reaching the shell** — commands launched via `attyx sessions` (e.g. reconnecting to a saved session) were silently dropped instead of being sent to the PTY. They now reach the shell correctly.
